[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "aim-native"
version = "0.1.0"
description = "Forked aim solver from apollo; lib path for alphaduck training data extraction"
requires-python = ">=3.9"

[tool.maturin]
features = ["pyo3/extension-module"]
