[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "graph-native"
version = "0.1.0"
description = "Rust native Orbit Wars agent whose forward model is env_model"
requires-python = ">=3.9"
