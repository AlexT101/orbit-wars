[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "simbot-simplified-native"
version = "0.1.0"
description = "Native Orbit Wars agent (nearest-sniper baseline + vendored engine) for simbot-simplified"
requires-python = ">=3.9"
