[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "rust-bot-native"
version = "0.1.0"
description = "Native Orbit Wars agent (nearest-sniper parity) for the rust_bot"
requires-python = ">=3.9"
