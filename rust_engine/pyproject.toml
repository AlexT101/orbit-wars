[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "orbit-wars-rust"
version = "0.1.0"
description = "Rust Orbit Wars engine for parity checks and RL rollouts"
requires-python = ">=3.9"
